apiVersion: v1
kind: Service
metadata:
  name: todolist-backend
  labels:
    app: todolist
    component: backend
spec:
  selector:
    app: todolist
    component: backend
  ports:
    - name: http
      protocol: TCP
      port: 3500
      targetPort: 3500
  type: ClusterIP